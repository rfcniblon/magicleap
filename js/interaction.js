// code pour le bouton Formulaire 
function askName() {
    var name = document.getElementById("name").value;
    const message = "Hello " + name + ", your registre es en cours de validation ! ";           
 alert(message);
}